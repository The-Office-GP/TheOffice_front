export interface LoginFormInput {
    email: string;
    password: string;
}

export interface RegisterFormInput {
    username: string;
    email: string;
    password: string;
    password_confirmation: string;
}